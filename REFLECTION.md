All my predictions matched the actual results. The main thing I noticed was how similar isEven and isOdd are, they use almost the same code, just flipped, so it was easy to mix them up if I wasn't careful.
What logic challenged your thinking?
isOdd and isEven were the trickiest, even though they're simple. I had to slow down and actually think about === 0 versus !== 0 instead of just copying one function and flipping a symbol.
How might this kind of toolkit be used in the real world?
Small reusable functions like these save developers from rewriting the same logic over and over. It also makes bugs easier to find, since each function only does one simple thing.
Time spent?
This whole exercise took about 1 hour, from writing the first function to finishing the reflection.
